<!--
  generated-by: tools/pdf2md/convert.py
  pymupdf: 1.27.1
  source: SDS13783 Z-Wave Transport-Encapsulation Command Class Specification.pdf
  section: "3.6.4 Building Blocks"
  pages: 58-67
-->
# 3.6.4 Building Blocks

S2 functionality is relying on a number of building blocks for different parts of the protocol.

## 3.6.4.1 ECDH Key pair generation

[Each S2 node has one or more ECDH key pairs used to setup a temporary secure channel for the Network Key exchange. Key pair generation is described in [27].](../03.09-transport-service-command-class-version-2.md#3925-losing-segmentcomplete)

[The ECDH private key MUST be created from 32 random bytes, which are generated using the PRNG function (3.6.4.6).](03.06.04-building-blocks.md#3646-pseudo-random-number-generator-prng) [The public key is calculated from the private key using Curve25519 [27].](../03.09-transport-service-command-class-version-2.md#3925-losing-segmentcomplete)

## 3.6.4.2 Key exchange overview

In the following, the term Node A refers to the including node (typically a primary controller or SIS) while the term Node B refers to the joining node.

 Inclusion Step 1: Create a shared secret between Node A and Node B Both nodes calculate a shared secret based on an Authenticated Elliptic Curve Diffie Hellman key exchange (AuthECDH). Node A takes as input the Public Key of B, and its own KeyPub_B Private Key,. Node B takes as input the Public Key of A, and its own KeyPriv_A KeyPub_A Private Key,. Both returning the same KeyPriv_B ECDH Shared Secret.

AuthECDH [is based on ECDH using Curve25519 [27]. Authentication is achieved through](../03.09-transport-service-command-class-version-2.md#3925-losing-segmentcomplete) o [user verification as specified in Section 3.6.6.2.](03.06.06-key-management.md#3662-ecdh-key-pairs-device-specific-key-and-user-verification)

 Inclusion Step 2: Derive shared symmetric key for key exchange To establish a temporary Network Key for AES128-CCM and CTR_DRBG, two steps are needed: To convert the into a 16-byte Pseudo Random Key (. ECDH Shared Secret PRK) o takes as input the along with CKDF-TempExtract ECDH Shared Secret and KeyPub_A KeyPub_B. Temporary symmetric keys are derived based on CKDF-TempExpand, by giving the, PRK o and as input. This returns the following keys: KeyPub_A KeyPub_B Temporary CCM Key, combined Encryption and Authentication Key, denoted  TempKeyCCM Temporary Personalization String, denoted  TempPersonalizationString.  Inclusion Step 3: Exchange permanent Network Keys To exchange one or several Permanent Network Key (), Singlecast Message Encapsulation is PNK used with temporary symmetric derived keys (and TempKeyCCM. TempPersonalizationString) All Permanent Network Key Exchanges are carried out using the temporary symmetric o key. All Permanent CCM Keys,, and, are o KeyCCM KeyMPAN PersonalizationString derived from the corresponding using PNK CKDF-NetworkKeyExpand All functions are based on. CKDF AES128-CMAC o  Singlecast Message Encapsulation: [The algorithm uses an authenticated encryption scheme conforming to AES128-CCM [24]. It is](../03.09-transport-service-command-class-version-2.md#3925-losing-segmentcomplete) used to encrypt and authenticate secure payloads. AES128-CCM takes the and a KeyCCM as input. The algorithm returns a CCM Authenticated Ciphertext. Nonce

[silabs.com | Building a more connected world.](https://www.silabs.com/) PAGE 58 OF 118 SDS13783-14 Z-Wave Transport-Encapsulation Command Class Specification 2020-07-06 A Nonce is a “Number used Once”. Nonces are initially exchanged between the two o nodes and then mixed into a Mixed Entropy Input,, using MEI CKDF-MEI-Extract and With both nodes holding the same, they use the CKDF-MEI-Expand. MEI MEI and as input into CTR_DRBG to generate a new Nonce. PersonalizationString  Multicast Message Encapsulation: Both singlecast and multicast frames use AES-128 CCM for encryption and authentication but multicast frames use a different algorithm to generate the IV. For multicast, the CCM IV is called the Multicast Pre-Agreed Nonce (. MPAN)

[MPANs are pushed from Node A, to multiple receiving nodes, B1, B2, etc. MPANs MUST be generated by Node A (see section 3.6.4.10) and MUST be distributed securely to each node B1, B2 etc. using](03.06.04-building-blocks.md#36410-mpan-nextnonce-generator) singlecast messages.

Implementations of the Security 2 Command Class MUST provide AES-128 cryptographic services in the following modes of operation:

 AES-128 “RAW” This is also known as the AES-Electronic Code Book (ECB) and defines the basic operation of encrypting a 16 Byte Plaintext into a 16 Byte Ciphertext using an encryption key. AES-128 is used as a foundation for the following modes.

 AES-128 CCM [The Counter with CBC-MAC (CCM) [24] is an authenticated encryption scheme.](../03.09-transport-service-command-class-version-2.md#3925-losing-segmentcomplete)

 AES-128 CMAC The Cipher based Message Authentication Code (CMAC) is an essential part of the Key Derivation functions and used to mix Nonce contributions for SPAN synchronization.

 AES-128 CTR_DRBG [The Counter mode Deterministic Random Byte Generator (CTR_DRBG) [25] is a block cipher](../03.09-transport-service-command-class-version-2.md#3925-losing-segmentcomplete) based Pseudo Random Number Generator (PRNG) that is used to create Initialization Vectors and Network Keys.

In addition to the AES modes, the following building blocks MUST also be provided:

 AuthECDH Authenticated Elliptic Curve Diffie Hellman key exchange. Provides the temporary shared key material for protecting the network key exchange during inclusion.

## 3.6.4.3 Core AES (AES)

The security layer MUST implement the AES-128 encryption algorithm. This algorithm encrypts a single 128-bit block of plaintext using the Advanced Encryption Standard, AES. It receives a 128-bit key and a 128-bit plaintext block as input and produces a 128-bit cipher text block.

[silabs.com | Building a more connected world.](https://www.silabs.com/) PAGE 59 OF 118 SDS13783-14 Z-Wave Transport-Encapsulation Command Class Specification 2020-07-06

![Figure 10, AES-ECB (Electronic Code Book)](assets/img-a07fdefe45.png)

## 3.6.4.4 AES-128 CCM Encryption and Authentication

[The payload in the S2 Message Encapsulation Command MUST be encrypted and authenticated using AES-128 CCM. For details about AES-128 CCM and message decryption and validation, refer to [24] and [26].](../03.09-transport-service-command-class-version-2.md#3925-losing-segmentcomplete)

CCM takes the following input:

 A combined encryption and authentication key denoted KeyCCM  A Nonce  A variable-length additional authenticated data structure (AAD)  A variable-length payload to encrypt and authenticate CCM yields as output:  An encrypted version of the payload combined with an authentication tag covering the payload and the AAD

[3.6.4.4.1](../03.09-transport-service-command-class-version-2.md#3925-losing-segmentcomplete) CCM profile [[26] defines a number of parameters that together determine the CCM profile used. S2 nodes MUST](../03.09-transport-service-command-class-version-2.md#3925-losing-segmentcomplete) use the following parameter values for the CCM profile:

 [Additional Authenticated Data (AAD) MUST be used (Adata = 1) The actual AAD is defined in section 3.6.4.4.2.](03.06.04-building-blocks.md#3644-aes-128-ccm-encryption-and-authentication)  The Length field MUST be 2 bytes long (L = 2 bytes)  The Authentication tag length MUST be 8 bytes (M = 8 bytes)  The Nonce length MUST be 13 bytes (N = 13 bytes)

The following length requirements MUST be observed:

 AAD structures of up to 30 bytes MUST be supported. Larger AAD structures MAY be supported.

[The 13 byte Nonce MUST be generated by taking the 13 most significant bytes of the](03.06.04-building-blocks.md#3648-nonces-for-ccm) NextNonce or [Nonce0 as described in section 3.6.4.8.](03.06.04-building-blocks.md#3648-nonces-for-ccm)

3.6.4.4.2 Additional Authenticated Data (AAD)

The following data structure MUST be used as AAD input for each CCM operation.

[silabs.com | Building a more connected world.](https://www.silabs.com/) PAGE 60 OF 118 SDS13783-14 Z-Wave Transport-Encapsulation Command Class Specification 2020-07-06

| 7 | 6 | 5 | 4 | 3 | 2 | 1 | 0 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Sender NodeID |  |  |  |  |  |  |  |
| Destination Tag |  |  |  |  |  |  |  |
| HomeID Byte 1 |  |  |  |  |  |  |  |
| … |  |  |  |  |  |  |  |
| HomeID Byte 4 |  |  |  |  |  |  |  |
| Message Length Byte 1 (MSB) |  |  |  |  |  |  |  |
| Message Length Byte 2 (LSB) |  |  |  |  |  |  |  |
| Sequence Number |  |  |  |  |  |  |  |
| Reserved |  |  |  |  |  | Enc Ext | Ext |
| Extension Data 1 |  |  |  |  |  |  |  |
| … |  |  |  |  |  |  |  |
| Extension Data M |  |  |  |  |  |  |  |

Sender NodeID (1 byte)

NodeID of the sending node.

Destination Tag (1 byte)

The use of this field depends on the actual frame.

If the field is used for a Singlecast frame, this field MUST carry the Receiver NodeID. If the field is used for an S2 Multicast frame, this field MUST carry the S2 Multicast Group ID.

HomeID (4 bytes)

HomeID of the sending node.

Message length (2 bytes)

[This field indicates the total length in bytes of the Security 2 Message Encapsulation Command.](03.06.05-message-encapsulation/03.06.05.03-message-encapsulation-commands.md#3653-message-encapsulation-commands)

Sequence Number (1 byte)

[Refer to the Security 2 Message Encapsulation Command (3.6.5.3.3).](03.06.05-message-encapsulation/03.06.05.03-message-encapsulation-commands.md#3653-message-encapsulation-commands)

Ext (1 bit)

[Refer to the Security 2 Message Encapsulation Command (3.6.5.3.3).](03.06.05-message-encapsulation/03.06.05.03-message-encapsulation-commands.md#3653-message-encapsulation-commands)

Enc Ext (1 bit)

[Refer to the Security 2 Message Encapsulation Command (3.6.5.3.3).](03.06.05-message-encapsulation/03.06.05.03-message-encapsulation-commands.md#3653-message-encapsulation-commands)

Reserved

[Refer to the Security 2 Message Encapsulation Command (3.6.5.3.3).](03.06.05-message-encapsulation/03.06.05.03-message-encapsulation-commands.md#3653-message-encapsulation-commands)

Extension Data (M bytes)

This field MUST contain all non-encrypted extension objects. [silabs.com | Building a more connected world.](https://www.silabs.com/) PAGE 61 OF 118 SDS13783-14 Z-Wave Transport-Encapsulation Command Class Specification 2020-07-06 This field MUST include the Length and Type fields prepending the actual data of each extension.

## 3.6.4.5 Message Authentication Code – AES-128 CMAC

![Figure 11, AES-128 CMAC building blocks](assets/img-b3dd9dc140.png)

[AES-128 CMAC is used for key derivation and Nonce mixing. The CMAC operation is specified in [23].](../03.09-transport-service-command-class-version-2.md#3925-losing-segmentcomplete)

## 3.6.4.6 Pseudo Random Number Generator (PRNG)

The PRNG MUST be used for:

 Generating new network keys when provisioning a new network.  Generating Nonce contributions for synchronizing the SPAN with peer nodes.

[The PRNG MUST be implemented as an AES-128 CTR_DRBG as specified in [25]. The following profile](../03.09-transport-service-command-class-version-2.md#3925-losing-segmentcomplete) MUST be used:

 No derivation function  No reseeding counter  Personalization string of 0x00 repeated 32 times  Output length = 16 bytes  security_strength is not used

[The entropy_input [25] for instantiating the PRNG MUST be generated by a truly random source, e.g.](../03.09-transport-service-command-class-version-2.md#3925-losing-segmentcomplete) white radio noise. The PRNG MUST be hardware seeded.

The inner state of the PRNG MUST be separated from the SPAN table.

## 3.6.4.7 Key extraction and derivation

The functions described in this section are used during key exchange.

[silabs.com | Building a more connected world.](https://www.silabs.com/) PAGE 62 OF 118 SDS13783-14 Z-Wave Transport-Encapsulation Command Class Specification 2020-07-06 3.6.4.7.1 CKDF-TempExtract

The CKDF-TempExtract function is used to extract the key entropy from the non-uniformly distributed ECDH Shared Secret. CKDF-TempExtract(Constant, ECDH Shared Secret, KeyPub_A, KeyPub_B) - PRK> PRK  The function’s input is defined by: o Constant = 0x33 repeated 16 times PRK o ECDH Shared Secret is the output of the ECDH key exchange o Public Keys of Nodes A and B o PRK = CMAC(Constant, ECDH Shared Secret | KeyPub_A | PRK KeyPub_B)

![Figure 12, CKDF-TempExtract function block diagram](assets/img-36b617071e.png)

3.6.4.7.2 CKDF-TempExpand

[Once the PRK has been computed, the temporary Authentication, Encryption and Nonce Keys MUST be](../03.09-transport-service-command-class-version-2.md#3925-losing-segmentcomplete) derived using the CKDF-TempExpand [function [22].](../03.09-transport-service-command-class-version-2.md#3925-losing-segmentcomplete)

CKDF-TempExpand(PRK, Constant) -> {TempKeyCCM, TE TempPersonalizationString}  The function’s input is defined by: o [PRK is calculated in the previous section 3.6.4.7.1](03.06.04-building-blocks.md#3647-key-extraction-and-derivation) o Constant = 0x88 repeated 15 times TE  Calculations are performed as follows: o T1 = CMAC(PRK, Constant | 0x01) TE o T2 = CMAC(PRK, T1 | Constant | 0x02) TE o T3 = CMAC(PRK, T2 | Constant | 0x03) TE  Output is defined as follows: o TempKeyCCM = T1. Temporary CCM Key, combined Encryption and Authentication Key. o TempPersonalizationString = T2 | T3

[silabs.com | Building a more connected world.](https://www.silabs.com/) PAGE 63 OF 118 SDS13783-14 Z-Wave Transport-Encapsulation Command Class Specification 2020-07-06

![Figure 13, CKDF-TempExpand function block diagram](assets/img-045fd7a7ed.png)

3.6.4.7.3 Permanent Key Exchange

A node that has been security bootstrapped into the network is characterized by being in possession of one or more Network Key(s). This key(s) is used throughout the lifetime of the network, typically measured in years to decades. Different nodes MAY have different Class Keys. A central controller node MUST manage all the keys and select at inclusion time which class key(s) to share with a given node.

[Network Keys for all Security 2 Classes MUST be 16 random bytes, generated by using the PRNG function described in section 3.6.4.6.](03.06.04-building-blocks.md#3646-pseudo-random-number-generator-prng)

3.6.4.7.3.1 Key Derivation

Once the Network Key(s) has been exchanged, the, and KeyCCM PersonalizationString values MUST be derived using the CKDF-NetworkKeyExpand [function [22].](../03.09-transport-service-command-class-version-2.md#3925-losing-segmentcomplete) KeyMPAN

CKDF-NetworkKeyExpand (PNK, Constant) -> {KeyCCM, NK PersonalizationString, KeyMPAN}  The function’s input is defined by: o PNK is the permanent network key. o Constant = 0x55 repeated 15 times NK  Calculations are performed as follow: o T1 = CMAC(PNK, Constant | 0x01) NK o T2 = CMAC(PNK, T1 | Constant | 0x02) NK o T3 = CMAC(PNK, T2 | Constant | 0x03) NK o T4 = CMAC(PNK, T3 | Constant | 0x04) NK  Output is obtained by: o KeyCCM = T1; CCM Key, combined Encryption and Authentication o PersonalizationString = T2 | T3 o KeyMPAN = T4

[silabs.com | Building a more connected world.](https://www.silabs.com/) PAGE 64 OF 118 SDS13783-14 Z-Wave Transport-Encapsulation Command Class Specification 2020-07-06

![Figure 14, CKDF-NetworkKeyExpand function block diagram](assets/img-e3186d280e.png)

## 3.6.4.8 Nonces for CCM

The Nonce is used for the CCM encryption in a Security 2 Message Encapsulation Command. Nonces provide these security benefits:  Replay protection. Replaying an old message will result in the replay being discarded because the Nonce has already been used.  The security proofs for CCM rely on the assumption that the same plaintext is never encrypted under the same key twice. Having unique Nonces upholds this assumption. Singlecast and Multicast transport use different mechanisms for generating Nonces as described in the following sections. Singlecast Pre-Agreed Nonces are referred to as. SPAN Multicast Pre-Agreed Nonces are referred to as. MPAN

## 3.6.4.9 SPAN NextNonce Generator

Singlecast Nonces MUST be generated in the following way: Skip steps 1 through 3 if a SPAN is already established 1. Exchange 16 bytes of Entropy between the Sender and the Receiver 2. Mix the entropy contributions 3. Instantiate CTR_DRBG and store the working state in the SPAN table 4. Generate 13 bytes of from the established using the Nonce SPAN NextNonce function. 5. Save the updated working state in the SPAN table

3.6.4.9.1 SPAN Instantiation The Sender and Receiver MUST instantiate the CTR_DRBG as follows: 1. The Sender and Receiver MUST exchange 16 bytes of Entropy Input (EI), resulting in 32 bytes of shared entropy a. [Both contributions MUST be generated using the PRNG (see Section 3.6.4.6)](03.06.04-building-blocks.md#3646-pseudo-random-number-generator-prng) 2. Mix the 32 bytes EI into, using and MEI CKDF-MEI-Extract CKDF-MEI-Expand functions The CTR_DRBG MUST be instantiated using the following profile: a. Entropy Input = MEI (obtained with CKDF-MEI_Expand) b. Personalization_String = PersonalizationString c. Output length = 16 d. No derivation function e. No reseeding counter [silabs.com | Building a more connected world.](https://www.silabs.com/) PAGE 65 OF 118 SDS13783-14 Z-Wave Transport-Encapsulation Command Class Specification 2020-07-06 f. No Security_strength 3. The CTR_DRBG now has its inner state set, referred to as the InnerSPAN

3.6.4.9.1.1 Mixing Mixing of the two EIs is done by first calling with the EIs as input and using the CKDF-MEI-Extract result as input for CKDF-MEI-Expand

3.6.4.9.1.1.1 CKDF-MEI-Extract CKDF-MEI-Extract(ConstNonce, SenderEI | ReceiverEI) -> NoncePRK  The Input is defined by: o ConstNonce = 0x26 repeated 16 times  The Output is obtained by: o NoncePRK = CMAC(ConstNonce, SenderEI | ReceiverEI)

3.6.4.9.1.1.2 CKDF-MEI-Expand CKDF-MEI-Expand(NoncePRK, ConstEntropyInput) -> MEI  The Input is defined by: o NoncePRK is the pseudo random value obtained in the Extract step. o ConstEntropyInput = 0x88 repeated 15 times  The Output is obtained by: o T0 = ConstEntropyInput | 0x00 o T1 = CMAC(NoncePRK, T0 | ConstEntropyInput | 0x01) o T2 = CMAC(NoncePRK, T1 | ConstEntropyInput | 0x02) o MEI = T1 | T2

3.6.4.9.2 Generation With the CTR_DRBG having its inner state set, new MUST now be generated by subsequent calls SPANs to the function. NextNonce

3.6.4.9.2.1 NextNonce The [function is defined as the CTR_DRBG_Generate_algorithm [25] with the following](../03.09-transport-service-command-class-version-2.md#3925-losing-segmentcomplete) NextNonce parameters:

 working_state = InnerSPAN  requested_number_of_bits = 128  additional_input = “” (empty string, i.e. zero bytes)

The NextNonce function MUST return a new for each call. The MUST be stored in SPAN InnerSPAN [the SPAN table as described in Section 3.6.5.1.1.](03.06.05-message-encapsulation/03.06.05.01-singlecast-messages-and-span-management.md#3651-singlecast-messages-and-span-management)

The 16 bytes of Nonce MUST be truncated to the 13 most significant bytes before passing to the CCM module.

## 3.6.4.10 MPAN NextNonce Generator

[Multicast Pre-Agreed Nonces (MPAN) are generated by encrypting successive values of a counter. The initial value MUST be a 16 byte random number generated by the PRNG (3.6.4.6). Whenever an MPAN](03.06.04-building-blocks.md#3646-pseudo-random-number-generator-prng) is generated and consumed by the CCM module, the inner MPAN state is incremented.

[MPAN instantiation and synchronization is described in section 3.6.5.2.](03.06.05-message-encapsulation/03.06.05.02-multicast-messages-and-mpan-management.md#3652-multicast-messages-and-mpan-management)

[silabs.com | Building a more connected world.](https://www.silabs.com/) PAGE 66 OF 118 SDS13783-14 Z-Wave Transport-Encapsulation Command Class Specification 2020-07-06 3.6.4.10.1 MPAN Generation

MPAN generation is needed when a node sends or receives a secure multicast message. The generation CC:009F.01.00.11.014 procedure, called, MUST comply with the following description: NextMPAN

1. The node reads the 16-byte inner MPAN state N from the MPAN table. 2. The node performs an encryption of the inner MPAN state N using AES-128-ECB KeyMPAN [(obtained during key derivation 3.6.4.7.3.1) as key. The result is MPAN N.](03.06.04-building-blocks.md#3647-key-extraction-and-derivation) 3. The node increments the inner MPAN state N. The result is the inner MPAN state N+1 which is stored in the MPAN table. The increment operation MUST treat the inner MPAN state as an unsigned 16-byte integer. Thus, incrementing all ones MUST yield all zeros.

![Figure 15, MPAN generation](assets/img-ddbea20c31.png)
