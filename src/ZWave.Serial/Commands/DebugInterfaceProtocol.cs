namespace ZWave.Serial.Commands;

/// <summary>
/// Debug interface protocol identifiers for radio debug commands.
/// </summary>
public enum DebugInterfaceProtocol : ushort
{
    /// <summary>
    /// Packet Trace Interface (PTI).
    /// </summary>
    Pti = 0x0000,
}
